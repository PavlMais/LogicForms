export const NodeTypes = Object.freeze({
    INFO_BLOCK: "InfoBlock",
    QUESTION_TEXT: "QuestionText",
    QUESTION_CHOISE: "QuestionChoise",
    START: "Start",
    END: "End"
});

export const BitwiseOp = Object.freeze({
    AND: "And",
    OR: "Or"
})

export const StrConditions = Object.freeze({
    EQUAL: "Equal",
    INCLUDE: "Include"
})

export const NumCondition = Object.freeze({
    EQUAL: "Equal",
    BETWEEN: "Between",
    GREATER:"Greater"
})